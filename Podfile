# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NDEtc' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NDEtc
  pod 'NDAutolayoutUtils', :path => '../NDAutolayoutUtils' # 0.0.7
  pod 'NDLog', :path => '../NDLog' # 0.0.7
  pod 'NDManualObjects', :path => '../NDManualObjects' # 0.0.7
  pod 'NDMVVM', :path => '../NDMVVM'
  pod 'NDUtils/objc', :path => '../NDUtils' # 0.0.7

  target 'NDEtcTests' do
    # Pods for testing
  end

end
