# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

workspace 'NDEtc'

target 'Samples' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NDEtc
  pod 'NDEtc', :path => '.'

  pod 'NDAutolayoutUtils', :path => '../NDAutolayoutUtils' # 0.0.8
  pod 'NDLog', :path => '../NDLog' # 0.0.7
  pod 'NDManualObjects', :path => '../NDManualObjects' # 0.0.7
  pod 'NDMVVM', :path => '../NDMVVM'
  pod 'NDUtils/objc', :path => '../NDUtils' # 0.0.7

  target 'SamplesTests' do
    # Pods for testing
  end

end
